(
	(platform ext)

	(def terminal_init # :: (
		% (term-fullscreen false)
		(term-print "Terminal size:" (tty-cols) "x" (tty-rows) "\n")
		(term-print "Welcome to ^bLivium^g :)\n^wtest\n")
	))
	(export terminal_init/0)
)

